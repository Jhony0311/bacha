import {StyleSheet} from 'react-native';
import colors from './colors.styles';

const base = StyleSheet.create({
    bigTitle: {
        fontSize: 26,
        color: colors.text
    }
});


export default base;
